# README images

Drop screenshots referenced by the top-level `README.md` here.

## `console.png`

A screenshot of the web console used in the README. Before committing:

- **Redact secrets.** Hide or rename real provider names, credential names, and
  any virtual keys. Never capture a revealed key.
- Use the **Overview** (or **Analytics**) tab — it shows the product without
  exposing configuration detail.
- Aim for a ~1400px-wide capture; PNG; keep it under ~500 KB.

Once `console.png` is added, the placeholder comment above the image tag in
`README.md` can be removed.
